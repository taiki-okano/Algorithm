#include <cstdio>
#include <algorithm>

constexpr int MAX_N = 100000;

int N, Q;
long long T;

long long A[MAX_N], target[MAX_N];
unsigned int D[MAX_N];

void solve();

int main(){
	std::fill(target, target + MAX_N, -1);

	scanf("%d%d%d", &N, &T, &Q);

	for(int i = 0; i < N; ++i){
		scanf("")
	}

	return 0;
}

void solve(){
	
}
